const { LOG_PATH, TESTS_PATH } = require('./filePath')
const {
  COMPARE_FUNCTIONS,
  COMPARE_FUNCTIONS_KEYS
} = require('./compareFunctions')

module.exports = {
  LOG_PATH,
  TESTS_PATH,
  COMPARE_FUNCTIONS,
  COMPARE_FUNCTIONS_KEYS
}
