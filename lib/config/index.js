const { TEXT, TEXT_KEYS } = require('./text.template')
const { ICONS, ICONS_KEYS, ICONS_VALUES } = require('./icon.template')

module.exports = {
    TEXT,
    TEXT_KEYS,
    ICONS,
    ICONS_KEYS,
    ICONS_VALUES
}
